import java.security.Permission;

class mp implements mu {
   private static Permission if;
   private static Permission a;
   private static Permission new;
   private static Permission int;
   private ThreadLocal for = new ThreadLocal();
   private ThreadLocal do = new ThreadLocal();

   static {
      if = new mt(mr.if, "threadLocalEcImplicitlyCa");
      a = new mt(mr.if, "ecImplicitlyCa");
      new = new mt(mr.if, "threadLocalDhDefaultParams");
      int = new mt(mr.if, "DhDefaultParams");
   }
}
