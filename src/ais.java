public enum ais {
   if,
   a,
   try,
   new,
   int,
   for;

   public static ais[] a() {
      return (ais[])do.clone();
   }

   public int a(double var1) {
      // $FF: Couldn't be decompiled
   }
}
