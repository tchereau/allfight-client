public class oj extends n4 {
   public void long() {
      if (this.try != null && !this.try.goto && this.try.m() % 20L == 0L) {
         this.a = this.else();
         if (this.a != null && this.a instanceof rn) {
            ((rn)this.a).J(this.try, this.new, this.int, this.for);
         }
      }

   }
}
