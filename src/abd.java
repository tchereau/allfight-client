public enum abd {
   if(15, "Common"),
   a(14, "Uncommon"),
   byte(11, "Rare"),
   try(13, "Epic"),
   do(6, "Unreal");

   public final int new;
   public final String int;

   public static abd[] a() {
      return (abd[])for.clone();
   }

   private abd(int var3, String var4) {
      this.new = var3;
      this.int = var4;
   }
}
